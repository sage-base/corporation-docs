[project]
name = "corporation-docs"
version = "0.1.0"
description = "Team documentation site"
requires-python = ">=3.10"
dependencies = [
    "mkdocs-material>=9.0.0",
]

[tool.uv]
dev-dependencies = []
